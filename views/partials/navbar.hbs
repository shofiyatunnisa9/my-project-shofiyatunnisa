<nav class="navbar navbar-expand-lg bg-body-tertiary">
    <div class="container-fluid">
        <a class="navbar-brand" href="#">
            <img src="/assets/img/logo-sho.png" width="90" height="60">
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent"
            aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <a class="nav-link 
                    {{#if (equal currentPage 'Home')}}active fw-bold{{/if}}" aria-current="page" href="/">Home</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link {{#if (equal currentPage 'MyProject')}}active fw-bold{{/if}}"
                        href="/myproject">My Project</a>
                </li>

                <li class="nav-item">
                    <a class="nav-link {{#if (equal currentPage 'Testimonials')}}active fw-bold{{/if}}"
                        href="/testimonials">Testimonials</a>
                </li>
            </ul>
            <div>
                {{#if user}}
                <span class="me-3">Selamat datang, {{user.name}}</span>
                <a href="/logout" class="me-3">Logout</a>
                {{/if}}
                <a class="btn btn-black {{#if (equal currentPage 'Contact')}}active fw-bold{{/if}}"
                    href="/contact">Contact
                    Me</a>
            </div>
        </div>
    </div>
</nav>